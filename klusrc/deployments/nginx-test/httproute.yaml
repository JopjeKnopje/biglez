apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httproute-ngnix
spec:
  parentRefs:
  - name: envoy
    namespace: envoy-gateway-system
  rules:
    - backendRefs:
      - name: nginx-service
        port: 80
