apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: envoy
  namespace: gateways
  annotations:
    cert-manager.io/issuer: envoy
spec:
  addresses:
    - value: 192.168.1.32
  gatewayClassName: envoy
  listeners:
    # - name: http
    #   protocol: HTTP
    #   # Used for cert-manager and HTTP->HTTPs redirects for all hosts
    #   port: 80
    #   allowedRoutes:
    #     namespaces:
    #       from: Selector
    #       selector:
    #         matchLabels:
    #           gateway-certmanager: allowed
    - name: main
      hostname: thuis.joppeboeve.nl
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: All
          # selector:
          #   matchLabels:
          #     app: nginx-test
      # tls:
      #   mode: Terminate
      #   certificateRefs:
      #     - name: "cert-auth-launchpad023-nl"
      #


    - name: main
      hostname: 192.168.1.32
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: All
